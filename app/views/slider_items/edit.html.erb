<div class="slider_item edit">
    <h1>Edit Slider Item <%= @slider_item.id %></h1>
    <%= form_for( @slider_item ) do |f| %>
	<%= render(:partial => 'form', :locals => {:f => f}) %>
	<div class="button-submit">
	    <%= f.submit("Обновить слайд") %>
	</div>

    <% end %>
</div>
