
<div class="post new">
  <h1>Create New Post</h1>
  <%= form_for( @post ) do |f| %>
  <%= render(:partial => 'form', :locals => {:f => f}) %>
	<%= f.submit("Create Post", :class => 'btn btn-primary') %>

  <% end %>
</div>
