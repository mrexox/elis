<div class="container">
<p>Точно удалить пост <%= @post.name%>?</p>
<%= form_for(@post, :method => 'delete') do |f| %>
	<%= image_tag(f.object.image_url.to_s, :class => 'image')%>
    <h3>Текст:</h3>
    <p><%=raw f.object.text%></p>
    <%= f.submit("Да")%>
	<% end %>
</div>
